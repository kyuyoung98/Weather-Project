import React, { useContext, useRef } from 'react';
import { useNavigate } from 'react-router-dom';

const Main = () => {
  return (
    <div>
        <h1>이게 메인 이지롱</h1>
    </div>
  );
}

export default Main;